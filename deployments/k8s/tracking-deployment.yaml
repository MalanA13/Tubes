apiVersion: apps/v1
kind: Deployment
metadata:
  name: tracking-deployment
  labels:
    app: tracking-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tracking-service
  template:
    metadata:
      labels:
        app: tracking-service
    spec:
      containers:
      - name: tracking-service
        # Jika menggunakan Azure (ACR), ubah image di bawah ini menjadi:
        # image: namakampusacr.azurecr.io/logistics-tracking:latest
        image: logistics-tracking:latest
        # Gunakan imagePullPolicy: Never atau IfNotPresent jika test di minikube lokal
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8083
        env:
        - name: PORT
          value: "8083"
        - name: TRACKING_SVC_URL
          value: "http://tracking-service:8083"
        - name: ORDER_SVC_URL
          value: "http://order-service:8081"
        - name: AUTH_SVC_URL
          value: "http://auth-service:8087"
---
apiVersion: v1
kind: Service
metadata:
  name: tracking-service
spec:
  selector:
    app: tracking-service
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
  type: ClusterIP
